<section class="promo-section" id="vip">
  <div class="background-overlay"></div>
  
  <div class="container">
    <!-- Texto Principal (Visible en ambos, posición cambia) -->
    <div class="header-content">
      <h2 class="main-title">
        <span class="text-p">Skip the Line.</span>
        <br>
        <strong class="text-main">Get the Best.</strong></h2>
      <p class="text-p subtitle">
        <strong>Reserve priority access to DP-M2 for just $1</strong> and lock in the lowest price of the entire campaign before Super Early Bird spots sell out.

      </p>
    </div>

    <!-- Tarjeta Blanca -->
    <div class="card">
      <div class="price-tag">
        <p class="text-p">$<span>1</span></p>
      </div>
      
      <ul class="features-list">
        <li class="feature-item">
          <div class="icon-check">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M13.6666 4L5.99992 11.6667L2.33325 8" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div class="feature-text">
            <h3 class="text-main">Guaranteed Super Early Bird pricing.</h3>
            <p class="text-p">Secure our best launch offer (up to 40% off).</p>
          </div>
        </li>

        <li class="feature-item">
          <div class="icon-check">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M13.6666 4L5.99992 11.6667L2.33325 8" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div class="feature-text">
            <h3 class="text-main">Batch #1 Priority Shipping</h3>
            <p class="text-p">Fast-track your order. Be the first to receive DP-M2.</p>
          </div>
        </li>

        <li class="feature-item">
          <div class="icon-check">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M13.6666 4L5.99992 11.6667L2.33325 8" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <div class="feature-text">
            <h3 class="text-main">Insider Updates</h3>
            <p class="text-p">Exclusive behind-the-scenes content and early announcements.</p>
          </div>
        </li>
      </ul>

      <a href="https://tu-enlace-aqui.com" target="_blank" rel="noopener noreferrer" class="text-main cta-button">
        Reserve My Spot Now
      </a>
      
      <p class="disclaimer text-p">*Priority spots will be limited.</p>
    </div>
  </div>
</section>

<style>
  /* Reset & Variables */
  :global(*) {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  :root {
    --color-bg: #1a1a1a;
    --color-text-main: #ffffff;
    --color-text-sub: #cccccc;
    --color-card-bg: #ffffff;
    --color-text-card: #111111;
    --color-primary: #007AFF; /* Azul similar a la imagen */
    --color-primary-hover: #0056b3;
    --font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
  }

  .promo-section {
    position: relative;
    width: 100%;
    background-image: url('assets/pricingOffer/bg.jpg'); /* Imagen de café de ejemplo */
    background-size: cover;
    background-position: center;
    font-family: var(--font-family);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem 1.5rem;
  }

  .background-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.6); /* Oscurecer fondo */
    z-index: 1;
  }

  .container {
    position: relative;
    z-index: 2;
    width: 100%;
    max-width: 1200px;
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  /* Header Content */
  .header-content {
    text-align: left;
    color: var(--color-text-main);
  }

   .header-content .main-title {
    font-size: 2.5rem;
    font-weight: 800;
    line-height: 1.1;
    margin-bottom: 1rem;
    letter-spacing: -0.02em;
  }
  .promo-section li:nth-child(3) {
    margin-bottom: 0
  }

  .promo-section .text-description {
    font-size: 24px;
    line-height: 1.5;
    max-width: 400px;
  }

  /* Card Styles */
  .card {
    background: var(--color-card-bg);
    border-radius: 24px;
    padding: 2.5rem 2rem;
    box-shadow: 0 20px 40px rgba(0,0,0,0.3);
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    width: 100%;
  }

  .price-tag {
    font-size: 5rem;
    font-weight: 300;
    color: var(--color-text-card);
    margin-bottom: 2rem;
    line-height: 1;
  }

  .price-tag,
  .price-tag .text-p,
  .price-tag .text-p span {
    align-self: center;
    width: 100%;
    text-align: center;
  }

  .features-list {
    list-style: none;
    width: 100%;
    text-align: left;
    margin-bottom: 56px;
  }

  .feature-item {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .icon-check {
    background-color: #111;
    border-radius: 50%;
    width: 24px;
    height: 24px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    margin-top: 2px;
  }

  .feature-text h3 {
    font-size: 1.1rem;
    font-weight: 700;
    color: var(--color-text-card);
    margin-bottom: 0.25rem;
  }

  .feature-text p {
    font-size: 0.9rem;
    color: #666;
    line-height: 1.4;
  }

  .cta-button {
    display: block;
    width: 100%;
    background-color: var(--color-primary);
    color: white;
    text-decoration: none;
    padding: 1rem 2rem;
    border-radius: 12px;
    font-weight: 600;
    font-size: 1rem;
    transition: background-color 0.2s ease, transform 0.1s ease;
    text-align: center;
    margin-bottom: 0.5rem;
  }

  .cta-button:hover {
    background-color: var(--color-primary-hover);
  }
  
  .cta-button:active {
    transform: scale(0.98);
  }

  .disclaimer {
    font-size: 0.75rem;
    color: #000;
    font-style: italic;
    text-align: center;
    width: 100%;
  }

  /* --- RESPONSIVE: DESKTOP (1440px) --- */
  @media (min-width: 1024px) {
    .promo-section {
      align-items: flex-start; /* Alinear arriba */
      padding-top: 10vh;
    }

    .container {
      flex-direction: row;
      align-items: center;
      justify-content: space-between;
      padding: 0 4rem;
    }

    .header-content {
      width: 45%;
      padding-top: 2rem;
    }

   .header-content .main-title,
   .header-content .main-title span,
   .header-content .main-title strong {
      font-size: 48px;
    }

    .card {
      width: 450px; /* Ancho fijo aproximado para la tarjeta */
      align-items: flex-start; /* Alinear texto a la izquierda en desktop */
      text-align: left;
      padding: 3rem;
      margin-top: -2rem; /* Efecto visual de superposición */
    }
    
    .price-tag .text-p {
      font-size: 96px;
    }
    .price-tag .text-p span{
      font-size: 144px;
    }

    .feature-item {
      margin-bottom: 2rem;
    }
  }

  /* Ajustes específicos para Mobile 390px */
  @media (max-width: 480px) {
    .main-title {
      font-size: 2.2rem;
    }
    
    .card {
      padding: 2rem 1.5rem;
      border-radius: 20px;
    }

    .price-tag .text-p {
      font-size: 96px;
    }
    .price-tag .text-p span{
      font-size: 128px;
    }
  }
</style>
