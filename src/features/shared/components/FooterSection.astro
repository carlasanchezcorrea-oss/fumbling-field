<footer class="footer-section">
  <div class="container">
    <div class="footer-content">
      <!-- Logo and Description Section -->
      <div class="text-section scroll-animate">
        <div class="logo">
          <img src="public/logo-vector.svg" alt="">
        </div>
        <p class="description text-p">
          Miaomada is a Japanese company dedicated to creating precision-engineered products that elevate everyday life. Rooted in craftsmanship and built for modern routines.
        </p>
      </div>

      <!-- Social Media Icons Section -->
      <div class="social-section scroll-animate">
        <div class="social-icons">
          <a href="https://shop.miaomada.co.jp/en/pages/about-us" target="_blank" rel="noopener noreferrer" class="social-icon" aria-label="Website">
            <img src="public/assets/footer/web.svg" alt="">
          </a>
          
          <a href="https://www.instagram.com/miaomada_eng?igsh=MTF6MWh6ZWpmMHQ5Nw==" target="_blank" rel="noopener noreferrer" class="social-icon" aria-label="Instagram">
            <img src="public/assets/footer/ig.svg" alt="">
          </a>
          
          <a href="https://www.tiktok.com/@miaomada_eng?_r=1&_t=ZS-94dwVWVjVLk" target="_blank" rel="noopener noreferrer" class="social-icon" aria-label="TikTok">
            <img src="public/assets/footer/tiktok.svg" alt="">
          </a>
          
          <a href="https://www.facebook.com/share/18BdrUutXu/" target="_blank" rel="noopener noreferrer" class="social-icon" aria-label="Facebook">
            <img src="public/assets/footer/fb.svg" alt="">
          </a>
        </div>
      </div>
    </div>
  </div>
</footer>

<style>
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  .footer-section {
    background-color: #1E3A5F;
    padding: 3rem 1.5rem;
    width: 100%;
    overflow: hidden;
  }

  .container {
    max-width: 1440px;
    margin: 0 auto;
    width: 100%;
  }

  .footer-content {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    align-items: flex-start;
    gap: 2rem;
  }

  /* Text Section - Vertical Flow */
  .text-section {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    width: 100%;
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.3s ease-out;
  }

  .text-section.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .logo {
    width: fit-content;
  }

  .description {
    color: rgba(255, 255, 255, 0.85);
    font-size: 0.9375rem;
    line-height: 1.6;
    max-width: 480px;
    font-weight: 400;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
  }

  /* Social Section - Horizontal Flow */
  .social-section {
    display: flex;
    flex-direction: row;
    width: 100%;
    justify-content: flex-start;
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.3s ease-out 0.1s;
  }

  .social-section.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .social-icons {
    display: flex;
    flex-direction: row;
    gap: 1rem;
    align-items: center;
  }

  .social-icon {
    width: 48px;
    height: 48px;
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease-out;
    cursor: pointer;
    text-decoration: none;
  }

  .social-icon:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 20px rgba(255, 255, 255, 0.2);
  }

  .social-icon:active {
    transform: translateY(-2px);
  }

  /* Desktop - 1440px */
  @media (min-width: 1024px) {
    .footer-section {
      padding: 4rem 3rem;
    }

    .footer-content {
      flex-direction: row;
      align-items: center;
    }

    .text-section {
      max-width: 600px;
      width: auto;
    }

    .logo svg {
      width: 220px;
      height: 48px;
    }

    .description {
      font-size: 1.0625rem;
      line-height: 1.7;
    }

    .social-section {
      justify-content: flex-end;
      align-content: center;
      width: auto;
    }

    .social-icon {
      width: 56px;
      height: 56px;
      border-radius: 14px;
    }

    .social-icon svg {
      width: 28px;
      height: 28px;
    }
  }

  /* Mobile - 390px */
  @media (max-width: 480px) {
    .footer-section {
      padding: 2.5rem 1.25rem;
    }

    .text-section {
      gap: 1.25rem;
    }

    .logo svg {
      width: 160px;
      height: 36px;
    }

    .description {
      font-size: 0.875rem;
      line-height: 1.6;
    }

    .social-icon {
      width: 44px;
      height: 44px;
      border-radius: 10px;
    }

    .social-icon svg {
      width: 22px;
      height: 22px;
    }
  }

  /* Specific adjustments for 390px */
  @media (max-width: 400px) {
    .footer-section {
      padding: 2rem 1rem;
    }

    .logo svg {
      width: 150px;
      height: 34px;
    }

    .description {
      font-size: 0.8125rem;
    }

    .social-icons {
      gap: 0.75rem;
    }

    .social-icon {
      width: 42px;
      height: 42px;
    }
  }
</style>

<script>
  // Intersection Observer for scroll animations
  document.addEventListener('DOMContentLoaded', () => {
    const observerOptions = {
      root: null,
      rootMargin: '0px',
      threshold: 0.1
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
          observer.unobserve(entry.target);
        }
      });
    }, observerOptions);

    const animatedElements = document.querySelectorAll('.scroll-animate');
    animatedElements.forEach(el => observer.observe(el));
  });
</script>