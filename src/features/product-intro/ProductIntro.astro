<section class="feature container-global" id="problem">
  <div class="container">
    <!-- BLOQUE 1 -->
    <div class="row">
      <div class="image">
        <div class="image-1"></div>
      </div>

      <div class="text">
        <h2 class="text-main">Still using a scoop?</h2>
        <h3 class="text-main">It's time for an upgrade</h3>
        <p class="text-description">
          The traditional way is messy, slow, and unnecessarily frustrating.
        </p>
      </div>
    </div>

    <!-- BLOQUE 2 -->
    <div class="row reverse">
      <div class="image">
        <div class="image-2"></div>
      </div>

      <div class="text">
        <h2 class="text-main">Why scoop when you can slide?</h2>
        <h3 class="text-main">Meet the DP-M2</h3>
        <p class="text-description">
          The precision your routine deserves.
          <br>
          One slide. Perfect portion. In
          seconds. No mess. No friction.
        </p>
      </div>
    </div>
  </div>
</section>

<!-- SCRIPT -->
<script>
  const rows = document.querySelectorAll(".row");

  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add("show");
        }
      });
    },
    { threshold: 0.2 },
  );

  rows.forEach((row) => observer.observe(row));
</script>

<style>
  /* SECTION */
  .feature {
    background: #fff;
    padding-top: 80px;
    padding-bottom: 80px;
  }

  /* ROW */
  .row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 60px;
    margin-bottom: 100px;

    opacity: 0;
    transform: translateY(40px);
    transition: all 0.8s ease;
  }
  .row:nth-child(2) {
    margin-bottom: 0px;
  }

  /* ANIMACIÓN */
  .row.show {
    opacity: 1;
    transform: translateY(0);
  }

  /* REVERSE */
  .reverse {
    flex-direction: row-reverse;
  }

  /* IMAGE */
  .image div {
    width: 612px;
    height: 538px;
    border-radius: 20px;
    background-size: cover;
    background-position: right center;
    background-repeat: no-repeat;
  }
  .image > .image-1 {
    background-image: url("/assets/productIntro/product-1.jpg");
  }
  .image > .image-2 {
    background-image: url("/assets/productIntro/product-2.jpg");
  }

  /* TEXT */
  .text {
    max-width: 500px;
    color: #000;
  }

  .text h2 {
    font-size: 36px;
    margin-bottom: 24px;
  }

  .text h3 {
    font-size: 20px;
    font-weight: bold;
    margin-bottom: 10px;
  }

  .text p {
    font-size: 16px;
  }

  /* ========================= */
  /* 📱 MOBILE (390px) */
  /* ========================= */

  @media (max-width: 768px) {
    .feature {
      padding-top: 90px;
      padding-bottom: 90px;
    }

    .row {
      flex-direction: column;
      gap: 20px;
      margin-bottom: 60px;
    }

    .reverse {
      flex-direction: column;
    }

    .text h2 {
      font-size: 34px;
    }

    /* IMAGE */
    .image div {
      width: 100%;
      max-width: 500px;
      max-width: fit-content;
    }
    .image {
      width: 100%;
    }
    .image div {
      width: 100%;
      min-width: 100%;
      height: 300px;
      background-position: top;
    }    
  }

  /* ========================= */
  /* 💻 DESKTOP GRANDE (1440+) */
  /* ========================= */

  @media (min-width: 1440px) {
    .feature {
      /* padding: 140px 120px; */
    }

    .text h2 {
      /* font-size: 44px; */
    }
  }
</style>
