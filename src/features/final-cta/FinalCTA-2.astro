---
import "./final-cta.css";
---

<section class="cta container-global" id="cta">
  <div class="cta-container">
    <!-- TEXTO -->
    <div class="cta-text reveal">
      <h3 class="text-h3">Ready to upgrade your routine?</h3>

      <p class="text-description">
        Be the first to know when DP-M2 goes live and secure your spot in line
        for Super Early Bird access.
      </p>
    </div>

    <!-- FORM -->
    <div class="container-form">
      <div id="error-message-container-02"></div>
      <form
        class="cta-form reveal delay"
        data-form="banner_02_form"
        id="lead-form_02"
      >
        <input
          type="email"
          name="email"
          placeholder="Enter your email"
          class="text-description"
          required
        />
        <button class="text-main" type="submit">Notify me</button>
      </form>
    </div>
  </div>
</section>

<script>
  const elements = document.querySelectorAll(".reveal");

  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add("show");
        }
      });
    },
    { threshold: 0.2 },
  );

  elements.forEach((el) => observer.observe(el));
</script>
<script>
  document.addEventListener("DOMContentLoaded", () => {
    const form_02 = document.getElementById("lead-form_02");
    if (!form_02) return;

    const emailInput = form_02.querySelector("input[name='email']");
    const button = form_02.querySelector("button[type='submit']");

    // 🔴 Dynamic error container
    let errorEl = form_02.querySelector(".error-message");
    let errorElcontainer = document.querySelector(
      "#error-message-container-02",
    );

    if (!errorEl) {
      errorEl = document.createElement("p");
      errorEl.className = "error-message";
      errorEl.style.color = "#ff4d4f";
      errorEl.style.fontSize = "14px";
      errorEl.style.marginTop = "6px";
      errorElcontainer.appendChild(errorEl);
    }

    form_02.addEventListener("submit", async (e) => {
      e.preventDefault();

      const email = emailInput?.value;
      const formName = form_02.dataset.form || "unknown_form";
      const eventId = "lead_" + Date.now();

      if (!email) return;

      errorEl.textContent = "";

      // 🔄 loading state
      button.disabled = true;
      button.textContent = "Sending...";

      try {
        const response = await fetch(
          "https://dpm2.miaomada.co.jp/api/leads-save.php",
          {
            method: "POST",
            headers: {
              "Content-Type": "application/json",
            },
            body: JSON.stringify({
              email,
              source: formName,
              event_id: eventId,
            }),
          },
        );

        const data = await response.json();

        if (!response.ok) {
          throw new Error("Error en la respuesta");
        }

        // 🔴 EMAIL DUPLICATE
        if (data.type === "duplicate") {
          errorEl.textContent = data.message;
          return;
        }

        // 🟢 ONLY IF NEW
        if (data.success === true) {
          sessionStorage.setItem("registrated", true);
          sessionStorage.setItem("email", emailInput?.value);
          sessionStorage.setItem("formName", formName);
          // 🧹 reset + redirect
          form_02.reset();

          window.location.href = "/thank-you-newsletter";
        }
      } catch (error) {
        console.error("🔥 Error:", error);
        errorEl.textContent = "Ocurrió un error. Inténtalo nuevamente.";
      } finally {
        button.disabled = false;
        button.textContent = "Notify me";
      }
    });
  });
</script>

<style scoped>
  .cta {
    background-image:
      linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)),
      url("/assets/cta/bg-cta-2.png");

    background-size: 115%;
    background-position: right center;
    background-repeat: no-repeat;
  }
  /* ========================= */
  /* 📱 MOBILE */
  /* ========================= */

  @media (max-width: 768px) {
    .cta {
      background-image:
        linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)),
        url("/assets/cta/bg-cta-2.png");

      background-size: cover;
      background-position: center center;
      background-repeat: no-repeat;
    }
    .container-form {
      width: 100%;
    }
    .cta-container {
      flex-direction: column;
      align-items: flex-start;
    }

    .cta-container .cta-text {
      width: 100%;
    }

    .cta-container .cta-text,
    .cta-text h3 {
      font-size: 36px;
    }
    .cta-text p {
      font-size: 16px;
      margin-bottom: 15px;
    }

    /* FORM */
    .cta-form {
      margin-left: 0;
      flex-direction: column;
      width: 100%;
    }

    .cta-form input,
    .cta-form button {
      width: stretch;
    }
    .cta-form .text-main {
      font-size: 16px !important;
    }
  }
</style>
